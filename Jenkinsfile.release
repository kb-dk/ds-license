library identifier: 'releasePipeline@DRA-2545-arkivblik-set-up-shared-lib', retriever: modernSCM(scm: [$class: 'GitSCMSource', remote: 'https://sbprojects.statsbiblioteket.dk/stash/scm/ist/jenkins-shared.git', credentialsId: 'crowd'],libraryPath: "Kuana") _

releasePipeline(
    project: "ds-license",
    buildToTrigger: "ds-present",
    // List only the versions this specific repo needs
    extraParameters: ['PARENT', 'SHARED', 'STORAGE']
)